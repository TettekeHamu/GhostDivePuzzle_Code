using UnityEditor;
using UnityEngine;

namespace TettekeKobo.EditorExtension
{
    /// <summary>
    /// GameSceneのSetUpをおこなってくれるボタン 
    /// </summary>
    public class GameSceneSetupButton : Editor
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
